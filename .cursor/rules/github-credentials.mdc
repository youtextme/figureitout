---
description: GitHub PAT for youtextme — Girish Mahadevan personal access token (classic PAT) (always on)
alwaysApply: true
---

# GitHub — Girish Mahadevan personal access token (classic PAT)

- Machine auth: **`youtextme`** via classic PAT stored at `~/.config/agent/github_pat`.
- Before `gh`, `git push`, or GitHub API: `source ~/.config/agent/github.env`
- **Never** commit or paste the token. **Never** ask the human for a GitHub key.
- Full access except delete-repo. Docs: `~/.config/agent/GITHUB_CREDENTIALS.md`
